Initialize global nomos binary and clean up legacy submodules

Reasoning:
- Removed legacy .nomos-commons submodule and .agent directories.
- Initialized global nomos hooks to replace local submodule.
- Updated config.yaml to point to correct task_repo.
